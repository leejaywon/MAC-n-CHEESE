eval/wandb/
wandb/
.cache/
__pycache__/
*.py[cod]
.DS_Store
.venv
.venv/

# Secrets — never commit real keys
.env
.env.local
.env.*.local

# Ralph loop logs (dev artifacts)
logs/

# Live-submission run artifacts (downloaded papers, generated reviews, run report)
submit_work/

# Fresh random-PDF smoke downloads, manifests, and generated reviews
eval/random_pdf_runs/
.reviewer_sources/
